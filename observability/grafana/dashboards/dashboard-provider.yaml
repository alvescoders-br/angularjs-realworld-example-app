# Grafana dashboard provisioning (S2, Fase 6, issue #8)
apiVersion: 1

providers:
  - name: conduit
    orgId: 1
    folder: Conduit
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
